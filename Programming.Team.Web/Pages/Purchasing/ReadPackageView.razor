@inherits ReactiveComponentBase<PackageViewModel>
@if(ViewModel != null)
{
    <MudStack Row="true">
        <MudText>
            @ViewModel.ResumeGenerations resume generations for $@ViewModel.Price
        </MudText>
        <MudButton OnClick="ViewModel.Purchase.BindCommand<MouseEventArgs>()">Purchase</MudButton>
    </MudStack>
    
}
@code {

}
